<li>
  {{!-- // if the burger is not devoured put in this list
  // -- give btton to devoured --}}
  {{name}}{{burger_name}}<p>test</p>
  <button class="toDevour" data-id="{{id}}" data-devour="{{devoured}}">Devour</button>
  {{!-- <button class="toDevour" data-id="{{this.id}}" data-devour="{{devoured}}">Devour</button> --}}

</li>